# Base yml for building the ML2019 environment

name: ml19

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.7
  - matplotlib
  - pandas
  - numpy
  - scipy
  - pytest
  - pytest-cov
